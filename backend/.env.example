# Backend configuration. Every variable is prefixed with KP_.
# Copy to .env for local development. NEVER commit the real .env.

# local | ci | production
# In production the interactive API docs are disabled.
KP_ENVIRONMENT=local

# Frontend configuration. Values here are PUBLIC: anything prefixed with VITE_
# is embedded in the JavaScript bundle. Never put a secret in this file.

# mock = in-memory fake backend (default while the API does not exist yet)
# http = real calls to /api on the same origin
VITE_API_MODE=mock
